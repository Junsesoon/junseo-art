# 📂 scripts
* modified at: 2026-06-15

* 이 폴더는 프로젝트 빌드, 배포, 에셋 최적화 등을 돕는 **자동화 유틸리티 스크립트**를 보관하는 용도로 사용됩니다
* ⚠️이 폴더 안의 스크립트들은 Node.js 환경에서 실행되도록 작성되었습니다(`fs`, `path` 모듈 등 사용). 따라서 React 프론트엔드 코드(`App.jsx`나 컴포넌트 등) 내부에서 직접 `import`하여 사용하면 브라우저 에러가 발생하므로 절대 불러오지 마세요

# files
* `compress.js`: raw 파일을 압축하여 static 파일로 최적화 하는 스크립트